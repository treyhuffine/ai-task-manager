'use client';

import { HeartbeatSettings } from '@/components/heartbeat/heartbeat-settings';

/** Settings > Heartbeat: the full heartbeat settings. See docs/heartbeat-spec.md §7.1. */
export function HeartbeatSection() {
  return <HeartbeatSettings />;
}
